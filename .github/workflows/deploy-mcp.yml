name: Deploy MCP Server

on:
  workflow_dispatch:
  push:
    paths:
      - 'mcp-server/**'

jobs:
  deploy:
    runs-on: [self-hosted, linux, x64]
    timeout-minutes: 5
    env:
      HTTP_PROXY: 'http://127.0.0.1:7890'
      HTTPS_PROXY: 'http://127.0.0.1:7890'

    steps:
      - name: Checkout
        run: |
          cd /opt/actions-runner/_work/developer_hos/developer_hos
          git fetch origin master --depth=1
          git reset --hard origin/master

      - name: Sync MCP code
        run: |
          mkdir -p /opt/mcp-harmonyos
          rsync -a --delete \
            /opt/actions-runner/_work/developer_hos/developer_hos/mcp-server/ \
            /opt/mcp-harmonyos/

      - name: Install system deps
        run: |
          apt-get update -qq
          apt-get install -y -qq python3-venv python3-pip

      - name: Setup venv & install deps
        run: |
          python3 -m venv /opt/mcp-harmonyos/venv
          /opt/mcp-harmonyos/venv/bin/pip install --upgrade pip \
            -i https://pypi.tuna.tsinghua.edu.cn/simple
          /opt/mcp-harmonyos/venv/bin/pip install -r /opt/mcp-harmonyos/requirements.txt \
            -i https://pypi.tuna.tsinghua.edu.cn/simple

      - name: Install systemd service
        run: |
          cp /opt/mcp-harmonyos/mcp-harmonyos.service /etc/systemd/system/
          systemctl daemon-reload
          systemctl enable mcp-harmonyos
          systemctl restart mcp-harmonyos

      - name: Verify
        run: |
          systemctl stop mcp-harmonyos 2>/dev/null || true
          echo "=== test import ==="
          /opt/mcp-harmonyos/venv/bin/python -c "from mcp.server.fastmcp import FastMCP; print('FastMCP OK')" 2>&1
          echo "=== test run (5s timeout) ==="
          timeout 5 /opt/mcp-harmonyos/venv/bin/python /opt/mcp-harmonyos/server.py 2>&1 || true
          echo "=== start service ==="
          systemctl start mcp-harmonyos
          sleep 2
          systemctl status mcp-harmonyos --no-pager
          curl -s http://127.0.0.1:8765/sse 2>&1 | head -5 || echo "SSE endpoint check done"
