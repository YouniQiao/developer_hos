---
title: "性能对比和最佳实践"
original_url: /docs/dev/game-dev/pgd-query-practice-0000002450561240
format: md
upstream_id: dev/game-dev/pgd-query-practice-0000002450561240
last_sync: 2026-06-07
sync_hash: aca24b2c
---
## 不同迭代方式的性能对比

```
var query = world.Query<Health, Velocity>();

// 方式1: 实体迭代（最慢，但最灵活）
foreach (var entity in query.Entities)
{
    ref var health = ref entity.GetComponent<Health>();
    ref var velocity = ref entity.GetComponent<Velocity>();
    // 处理逻辑...
}

// 方式2: ForEachEntity（中等性能，易用性好）
query.ForEachEntity((ref Health health, ref Velocity velocity, IEntity entity) => {
    // 处理逻辑...
});

// 方式3: ArchetypeChunk（最快，但需要更多代码）
foreach (var (healthSegment, velocitySegment, entities) in query.ArchetypeChunk)
{
    var healths = healthSegment.Span;
    var velocities = velocitySegment.Span;
    for (int i = 0; i < healthSegment.Length; i++)
    {
        // 直接数组访问，最佳性能
    }
}
```
