---
title: "字体大小调节"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/set_app_font_size-0000002235941282
---

## 场景介绍

本示例通过滑动组件[Slider](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-components-slider)与[@ohos.data.preferences](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-data-preferences)实现应用内的字体大小调节，并实现屏蔽系统字体大小设置功能，用户可以按需调整当前阅读内容字体大小。

## 效果预览

![](./img/69784bb9.png "点击放大")

## 实现思路

1. 首选项为应用提供Key-Value键值型的数据处理能力，在首选项工具类PreferencesUtil中实现设置和读取字体大小的功能。

   ```
   // 在首选项中设置字体大小
   saveChangeFontSize(fontSize: number) {
     try {
       let fontPreferences: preferences.Preferences = this.getFontPreferences();
       fontPreferences.putSync(KEY_APP_FONT_SIZE, fontSize);
       fontPreferences.flushSync();
     } catch (err) {
     // ...
     }
   }
   // 从首选项中读取字体大小
   getChangeFontSize(): number {
     let defaultFontSize: number = 0;
     let fontPreferences: preferences.Preferences = this.getFontPreferences();
     let fontSize: number = fontPreferences.getSync(KEY_APP_FONT_SIZE, defaultFontSize) as number;
     return fontSize;
   }
   ```
2. 使用[Slider](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-components-slider)组件实现字体大小的滑动选择，每次滑动后调用[@ohos.data.preferences](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-data-preferences)对最新字体大小进行存储。

   ```
   Slider({
     value: this.changeFontSize === CommonConstants.SET_SIZE_HUGE
       ? CommonConstants.SET_SLIDER_MAX : this.changeFontSize,
     min: CommonConstants.SET_SLIDER_MIN, // 滑动最小值
     max: CommonConstants.SET_SLIDER_MAX, // 滑动最大值
     step: CommonConstants.SET_SLIDER_STEP, // 步长
     // 其他设置
   })
     .onChange((value: number) => {
       if (this.changeFontSize === 0) {
         this.changeFontSize = PreferencesUtil.getChangeFontSize();
         return;
       }
       this.changeFontSize = value
       PreferencesUtil.saveChangeFontSize(this.changeFontSize);
       this.changeFontSize = PreferencesUtil.getChangeFontSize()
     })
   ```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 工程目录

```
├──entry/src/main/ets                         // 代码区
│  ├──constants
│  │  ├──CommonConstants.ets                  // 通用常量类
│  │  └──StyleConstants.ets                   // 样式常量类
│  ├──database
│  │  └──PreferencesUtil.ets                  // 用户首选项
│  ├──entryability
│  │  └──EntryAbility.ets                     // 程序入口
│  ├──model
│  │  └──DataModel.ets                        // 接口数据
│  ├──pages
│  │  ├──NewsRead.ets                         // 新闻显示主页
│  │  └──SetFontSizePage.ets                  // 字体大小调节页
│  ├──utils
│  │  ├──GlobalContext.ets                    // 管理全局上下文
│  │  └──Logger.ets                           // 日志
│  └──view
│     ├──MoreFunctionCustomDialog.ets         // 更多功能弹窗
│     ├──News.ets                             // 新闻组件
│     ├──SliderLayout.ets                     // 滑块组件
│     └──TitleBar.ets                         // 标题组件
└──entry/src/main/resources                   // 应用资源目录
```

## 参考文档

[Slider](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-components-slider)

[@ohos.data.preferences（用户首选项）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-data-preferences)

## 代码下载

[字体大小调节示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260225161637.28106010954502052807825380753550%3A50001231000000%3A2800%3A54F9D7FCF4BA1A029B82E351B1FC08AA7D0E73EA79554F039BB46A82BB1B9103.zip?needInitFileName=true)
