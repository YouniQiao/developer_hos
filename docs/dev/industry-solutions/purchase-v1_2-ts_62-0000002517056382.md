---
title: "@BuilderParam如何接收父组件的状态变量"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/purchase-v1_2-ts_62-0000002517056382
---

## 问题现象

如何通过@BuilderParam装饰器传递@State装饰的状态变量？

## 效果预览

![](./img/41eb1464.png "点击放大")

## 背景知识

[@BuilderParam装饰器](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-builderparam)用于装饰指向[@Builder](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-builder)方法的变量，能够声明任意UI描述的元素。@BuilderParam装饰器初始化的值必须为@Builder，否则编译时就会输出报错信息。

## 解决方案

@State装饰器装饰的变量不可直接传递给@BuilderParam装饰器，可以使用@Builder装饰器当做桥梁，将状态变量传递给@Builder，使@BuilderParam装饰器包含状态变量的@Builder，实现状态变量内容的传递，具体实现如下：

* 在build方法中，调用MyCustomContainer子组件，并传入自定义构建函数。在MyCustomContainer传参时，通过箭头函数调用this.customView()和this.customView2()，实现子组件对父组件参数的调用。

  ```
  @Entry
  @Component
  struct TransmitBuilderParam {
    message: string = 'slot1';

    @Builder
    customPar(message: string) {
      Text(message);
    }

    build() {
      Row() {
        Column() {
          MyCustomContainer({
            customView: () => {
              this.customPar(this.message);
            }, customView2: () => {
              this.customPar('slot2');
            }
          });
        }
        .width('100%');
      }
      .height('100%');
    }
  }
  ```
* 在MyCustomContainer组件中，定义空自定义构建函数customViewBuild和customViewBuild2。使用@BuilderParam装饰器接收父组件函数并赋值给子组件中的customViewBuild和customViewBuild2，并且在子组件中，调用this.customView()和this.customView2()插入自定义内容。

  ```
  @Component
  struct MyCustomContainer {
    @Builder
    customViewBuild() {
    }

    @BuilderParam customView: () => void = this.customViewBuild;

    @Builder
    customViewBuild2() {
    }

    @BuilderParam customView2: () => void = this.customViewBuild2;

    build() {
      Column() {
        Text('childBeginText');
        this.customView();
        Text('-----不同位置测试-----');
        this.customView2();
        Text('childEndText');
      };
    }
  }
  ```

完整示例代码如下：

```
@Entry
@Component
struct TransmitBuilderParam {
  message: string = 'slot1';

  @Builder
  customPar(message: string) {
    Text(message);
  }

  build() {
    Row() {
      Column() {
        MyCustomContainer({
          customView: () => {
            this.customPar(this.message);
          }, customView2: () => {
            this.customPar('slot2');
          }
        });
      }
      .width('100%');
    }
    .height('100%');
  }
}

@Component
struct MyCustomContainer {
  @Builder
  customViewBuild() {
  }

  @BuilderParam customView: () => void = this.customViewBuild;

  @Builder
  customViewBuild2() {
  }

  @BuilderParam customView2: () => void = this.customViewBuild2;

  build() {
    Column() {
      Text('childBeginText');
      this.customView();
      Text('-----不同位置测试-----');
      this.customView2();
      Text('childEndText');
    };
  }
}
```
