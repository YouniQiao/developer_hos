---
title: "UTF8格式字符串转GB2312格式"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/format_conversion-0000002516884120
---

## 场景介绍

字符串编码格式转换是文本处理的典型场景之一，通过字符串编码格式转换能够解决跨平台、跨语言或不同系统间文本兼容性问题。

本示例使用ICU4C库，实现UTF8格式字符串转GB2312格式。

## 效果预览

![](./img/2bc8ec20.png "点击放大")

## 实现思路

通过调用ICU4C库中的ucnv\_convertEx()方法实现UTF-8→Unicode→GB2312的转换。

```
int Utf8ToGb2312(CharsetConverter* converter, const char* utf8Str, int utf8Len, char** gb2312Str, size_t* gb2312Len)
{
    UErrorCode status = U_ZERO_ERROR;

    if (!utf8Str || utf8Len <= 0) {
        return -1;
    }

    int32_t targetCapacity = utf8Len * 2;
    char* target = (char*)malloc(targetCapacity + 1);
    if (!target) {
        return -1;
    }

    const char* sourcePptr = utf8Str;
    const char* sourceLimit = utf8Str + utf8Len;
    char* targetPtr = target;
    char* targetLimit = target + targetCapacity;

    status = U_ZERO_ERROR;
    ucnv_convertEx(converter->gb2312Conv, converter->utf8Conv, &targetPtr, targetLimit, &sourcePptr, sourceLimit, NULL,
        NULL, NULL, NULL, TRUE, TRUE, &status);

    if (U_FAILURE(status) && status != U_BUFFER_OVERFLOW_ERROR) {
        free(target);
        OH_LOG_ERROR(LOG_APP, "UTF-8 to GB2312 conversion failed: %{public}s.", u_errorName(status));
        return -1;
    }

    *targetPtr = '\0';
    *gb2312Len = targetPtr - target;
    *gb2312Str = target;

    return 0;
}
```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 工程目录

```
├──entry/src/main/ets                  // 代码区
│  ├──entryability
│  │  └──EntryAbility.ets
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  └──pages
│     └──Index.ets                     // 主页面
├──entry/src/main/cpp                  // 代码区
│  ├──FormatConversion.h               // UTF8转GB2312类
│  └──FormatConversion.cpp             // UTF8转GB2312类实现
└──entry/src/main/resources            // 应用资源目录
```

## 参考文档

[ICU4C](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/icu4c)

## 代码下载

[UTF8格式字符串转GB2312格式示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260317201847.33902392753650404230518168880602%3A20260604160226%3A2800%3A07815891B514344BDAB46F4E76C7BFDB6053A09A2F1513992B20C335424C85D7.zip?needInitFileName=true)
