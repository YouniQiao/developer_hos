---
title: "视频拼接创作"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/merge_video-0000002297453758
---

## 场景介绍

视频拼接创作是影音娱乐类应用的高频使用场景之一，如用户需要调整视频顺序，完成多视频拼接创作。

本示例使用[List](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-container-list)组件结合[单一手势](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-gesture-events-single-gesture)实现视频元素的拖动排序，使用三方库[@ohos/mp4parser](https://ohpm.openharmony.cn/#/cn/detail/@ohos%2Fmp4parser)实现视频格式化以及视频合并。

## 效果预览

![](./img/e2df6ac0.png)

## 实现思路

1. 该方案基于mp4parser@2.0.7，执行ohpm install @ohos/mp4parser@2.0.7命令安装mp4parser三方库。
2. 使用[List](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-container-list)组件结合[单一手势](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-gesture-events-single-gesture)实现视频拖拽排序动效，拖拽排序同时更新视频拼接顺序。

   ```
   List() {
     ForEach(this.list, (item: string, index: number) => {
       ListItem() {
         // ...
         // 内部组件
       }
       .gesture(
         // ...
         // 处理拖拽排序事件
       );
     }, (item: string) => {
       return item;
     });
   }
   ```

3. 使用三方库[@ohos/mp4parser](https://ohpm.openharmony.cn/#/cn/detail/@ohos%2Fmp4parser)调用FFmpeg指令实现视频格式化以及视频拼接。

   ```
   // 视频批量合成
   MP4Parser.videoMultMerge(
     this.videoTxt,
     this.videoDir + '/' + this.outVideo,
     {
       callBackResult: (code: number) => {
         // ...
         // 回调事件
       },
     }
   );
   // 视频格式化
   MP4Parser.ffmpegCmd(
     `ffmpeg -i ${this.videoDir}/${videoMp4Name} -c copy -f mpegts ${this.videoDir}/${videoTmpTsName}`,
     {
       callBackResult: (code: number) => {
         // ...
         // 回调事件
       },
     }
   );
   // 视频旋转
   MP4Parser.ffmpegCmd(
     `ffmpeg -i ${this.videoDir}/${videoTmpTsName} ${transposeCmd}-c:a copy ${this.videoDir}/${videoTsName}`,
     {
       callBackResult: (code: number) => {
         // ...
         // 回调事件
       },
     }
   );
   ```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 工程目录

```
├──entry/src/main/ets              // 代码区
│  ├──common
│  │  └──Constants.ets             // 常量类
│  ├──entryability
│  │  └──EntryAbility.ets          // 程序入口类
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──pages
│  │  ├──MainPage.ets              // 主界面
│  │  └──VideoList.ets
│  └──utils
│     ├──CacheUtils.ets
│     ├──Logger.ets
│     ├──PhotoUtils.ets
│     └──VideoUtils.ets
└──entry/src/main/resources        // 应用资源目录
```

## 参考文档

[@ohos/mp4parser](https://ohpm.openharmony.cn/#/cn/detail/@ohos%2Fmp4parser)

[List](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-container-list)

[单一手势](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-gesture-events-single-gesture)

## 代码下载

[视频拼接创作示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260317194127.12604427065276762343600818977333%3A20260604141747%3A2800%3AF40405854E017B10D34D70618378D216A294F30A9104B317C4FB8C58EAF6E7F9.zip?needInitFileName=true)
