---
title: "截屏事件监听及分享"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/screenshot_listen-0000002261676460
---

## 场景介绍

截屏分享是各类应用中的高频使用场景之一，如应用检测到用户截屏操作，会弹出分享框提示。

本示例基于[@ohos.window](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window)，实现监听系统截屏事件的功能，并支持截图保存到相册和分享。

## 效果预览

![](./img/287c7233.png "点击放大")

## 实现思路

1. 在需要的页面使用[on('screenshot')](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window-window#onscreenshot9)实现监听系统截屏事件。
2. 通过[CustomDialog](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-methods-custom-dialog-box)打开分享弹窗。

   ```
   aboutToAppear(): void {
     try {
       this.windowClass.on('screenshot', async () => {
         this.pixmap = await this.screenshot();
         this.shareDialogController?.close();
         this.shareDialogController?.open();
       });
     } catch (exception) {
       // ...
     }
   }

   aboutToDisappear(): void {
     this.windowClass.off('screenshot');
   }
   ```
3. 通过[imagePacker.packToFile()](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-image-imagepacker#packtofile11)的方法将ImageSource图片源编码后直接打包进文件实现图片保存到相册。

   ```
   photoAccessHelper.getPhotoAccessHelper(context)
     .showAssetsCreationDialog(srcFileUris, photoCreationConfigs)
     .then((desFileUris: Array<string>) => {
       let imageFile1 = fileIo.openSync(desFileUris[0], fileIo.OpenMode.CREATE | fileIo.OpenMode.READ_WRITE);
       let srcPath = context.filesDir + CommodityConstants.IMAGE_URL;
       let dstPath = imageFile1.fd;
       try {
         fileIo.copyFileSync(srcPath, dstPath);
         fileIo.closeSync(imageFile1.fd);
         uiContext?.getPromptAction().showToast({ message: '保存成功' });
       } catch (error) {
         uiContext?.getPromptAction().showToast({ message: '保存失败' });
       }
     }).catch((error: BusinessError) => {
     uiContext?.getPromptAction().showToast({ message: '保存失败' });
   });
   ```
4. 通过[systemShare](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/share-system-share)实现图片分享。

   ```
   // 获取精准的utd类型
   let utdTypeId = utd.getUniformDataTypeByFilenameExtension('.png', utd.UniformDataType.IMAGE);
   let shareData: systemShare.SharedData = new systemShare.SharedData({
     utd: utdTypeId,
     uri: fileUri.getUriFromPath(filePath),
     title: '图片标题', // 不传title字段时,显示图片文件名
     description: '图片描述', // 不传description字段时,显示图片大小
   });
   // 进行分享面板显示
   let controller: systemShare.ShareController = new systemShare.ShareController(shareData);
   controller.show(context, {
     selectionMode: systemShare.SelectionMode.SINGLE,
     previewMode: systemShare.SharePreviewMode.DETAIL,
   });
   ```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 权限说明

* 获取位置信息权限：[ohos.permission.LOCATION](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all-user#ohospermissionlocation)。
* 获取模糊位置信息权限：[ohos.permission.APPROXIMATELY\_LOCATION](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all-user#ohospermissionapproximately_location)。

## 工程目录

```
├──common/common/src/main/ets                 // common包
│  ├──components
│  │  ├──CommodityList.ets                    // 商品清单
│  │  ├──CounterProduct.ets                   // 反效果产品
│  │  ├──EmptyComponent.ets                   // 空组件
│  │  └──ShippingAddress.ets                  // 收货地址
│  ├──constants
│  │  ├──BreakpointConstants.ets              // 断点常量
│  │  ├──CameraConstants.ets                  // 相机常量
│  │  ├──GridConstants.ets                    // 网格常量
│  │  └──StyleConstants.ets                   // 风格常量
│  ├──service
│  │  └──AlbumService.ets                     // 专辑服务
│  ├──utils
│  │  ├──BreakpointSystem.ets                 // 断点系统
│  │  ├──CommonDataSource.ets                 // 通用数据源
│  │  ├──LocalDataManager.ets                 // 本地数据管理器
│  │  ├──LocationManager.ets                  // 位置管理
│  │  ├──Logger.ets                           // 日志打印
│  │  └──Utils.ets                            // 工具类
│  └──viewmodel
│     ├──CommodityModel.ets                   // 商品模型
│     ├──OrderModel.ets                       // 订单模型
│     ├──ProductModel.ets                     // 产品模型
│     └──ShopData.ets                         // 商品数据
├──commons/common/src/main/resources          // commons包资源目录
│
├──entry/src/main/ets
│  ├──components
│  │  ├──CapsuleGroupButton.ets               // 按钮组件
│  │  ├──CommodityDetail.ets                  // 商品详情组件
│  │  └──SpecificationDialog.ets              // 弹窗组件
│  ├──constants
│  │  └──CommodityConstants.ets               // 常量
│  ├──entryability
│  │  └──EntryAbility.ets                     // 程序入口
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──pages
│  │  └──Index.ets                            // 主页
│  ├──utils
│  │  ├──SaveImage.ets                        // 保存图片到相册方法
│  │  └──ShareOperations.ets                  // 分享
│  └──viewmodel
│     ├──CommodityDetailData.ets              // 商品详情数据
│     └──TypeModel.ets                        // 数据类型
└──entry/src/main/resources                   // 应用资源目录
```

## 参考文档

[@ohos.window（窗口）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window)

[自定义弹窗(CustomDialog)](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-methods-custom-dialog-box)

[如何将PixelMap保存到相册](https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-image-20)

[分享图片](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/share-utd-image)

## 代码下载

[截屏事件监听及分享示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260210100038.50172567812991392944545362330129%3A50001231000000%3A2800%3AAE7F5B4F096C597801855B0A8C857539B9EC0C2DC22035300FF634C564F7734F.zip?needInitFileName=true)
