---
title: "VPN长时任务通知"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/vpn_adaptation-0000002507518793
---

## 场景介绍

VPN长时任务通知是各类应用中的高频使用场景之一。

本示例使用[backgroundTaskManager](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-resourceschedule-backgroundtaskmanager)后台任务管理申请后台任务，使用定时器模拟数据传输长时任务，通过[NotificationManager](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-notificationmanager)通知模板更新并发送通知。

## 效果预览

![](./img/e0880f42.webp "点击放大")

## 实现思路

1. 通过[getWantAgent](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-app-ability-wantagent#wantagentgetwantagent-1)方法获取WantAgent对象，申请长时任务。

   ```
   startContinuousTask() {
     // 现已不推荐三方应用使用显式Want拉起其他应用，本示例需要获取WantAgent对象，用于申请长时任务
     let wantAgentInfo: wantAgent.WantAgentInfo = {
       wants: [
         {
           bundleName: bundleName,
           abilityName: abilityName
         }
       ],
       actionType: wantAgent.OperationType.START_ABILITY,
       requestCode: 0,
     };
     // 通过wantAgent模块下getWantAgent方法获取WantAgent对象，用于申请长时任务
     wantAgentUtil.createWantAgent(Constants.BUNDLE_NAME, Constants.BUNDLE_NAME)
       .then((wantAgentObj: WantAgent) => {
         let list: string[] = ['dataTransfer'];
         backgroundTaskManager.startBackgroundRunning(this.context, list, wantAgentObj)
           .then((res: backgroundTaskManager.ContinuousTaskNotification) => {
             // 使用res中返回的notificationId来更新通知
             this.notificationId = res.notificationId;
           });
       });
   }
   ```
2. 使用[定时器](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-timer)对[NotificationManager](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-notificationmanager)进行定时通知更新。

   ```
   setInterval(() => {
     process++;
     this.updateContinuousTask(process);
   }, Constants.INTERVAL);

   updateContinuousTask(process: number) {
     // 任务进度满，模拟长时任务时间结束，停止任务
     if (process >= 100) {
       this.stopContinuousTask();
       return;
     }
     // 更新任务通知
     let request = updateNotification(this.notificationId, process);
     notificationManager.publish(request);
   }

   updateNotification = (id: number, process: number) => {
     // 应用定义下载类通知模版
     let downLoadTemplate: notificationManager.NotificationTemplate = {
       name: 'downloadTemplate', // 当前仅支持downloadTemplate，保持不变
       data: {
         title: 'xxx', // 必填
         fileName: 'xxx', // 必填
         progressValue: process, // 应用更新进度值，自定义
       }
     };
     let request: notificationManager.NotificationRequest = {
       id: id, // 必须是申请长时任务返回的id，否则应用更新通知失败
       content: {
         // 系统实况类型，保持不变
         notificationContentType: notificationManager.ContentType.NOTIFICATION_CONTENT_SYSTEM_LIVE_VIEW,
         systemLiveView: {
           typeCode: 8, // 上传下载类型需要填写8，当前仅支持此类型，保持不变
           title: `xxx`, // 应用自定义
           text: `xxx`, // 应用自定义
         }
       },
       updateOnly: true,
       notificationSlotType: notificationManager.SlotType.LIVE_VIEW, // 实况窗类型，保持不变
       template: downLoadTemplate
     };
     return request;
   }
   ```
3. 长时任务结束后，计算任务时间并发布文本类型通知。

   ```
   publishNotification = async (linkTime: number[]) => {
     // 计算任务时间，并转换为对应时间格式
     let duration = linkTime[1] - linkTime[0];
     let timeStr = this.formatTime(duration);
     let notificationWantAgent = await wantAgentUtil.createWantAgent(Constants.BUNDLE_NAME, Constants.ABILITY_NAME);
     let request: notificationManager.NotificationRequest =
       {
         id: 1, // 通知id，默认为1
         notificationSlotType: notification.SlotType.CONTENT_INFORMATION,
         content: {
           notificationContentType: notification.ContentType.NOTIFICATION_CONTENT_BASIC_TEXT, // 通知内容类型
           normal: {
             title: 'vpn连接断开',
             text: '连接时长: ' + timeStr,
           } // 基本类型通知内容
         },
         wantAgent: notificationWantAgent
       };
     notificationManager.publish(request);
   }
   ```

![](./img/af7e63fc.png)

本示例主要展示VPN开启期间长时任务通知的申请与更新功能，若涉及VPN相关功能问题请参见[VPN应用](https://gitcode.com/openharmony/applications_app_samples/tree/master/code/DocsSample/NetWork_Kit/NetWorkKit_NetManager/VPNControl_Case)。

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 权限说明

* 获取Internet网络权限：[ohos.permission.INTERNET](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all#ohospermissioninternet)。
* 获取Service Ability在后台持续运行权限：[ohos.permission.KEEP\_BACKGROUND\_RUNNING](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all#ohospermissionkeep_background_running)。

## 工程目录

```
├──entry/src/main/cpp               // 业务底层实现
├──entry/src/main/ets               // 代码区
│  ├──constants
│  │  └──Constants.ets              // 常量
│  ├──entryability
│  │  └──EntryAbility.ets
│  ├──pages
│  │  └──MainPage.ets               // 主页
│  └──utils
│     ├──Logger.ets                 // 日志工具
│     ├──NotificationPublish.ets    // 通知发布工具
│     ├──Toast.ets                  // 气泡弹窗
│     ├──VPNUtils.ets               // VPN功能实现
│     └──WantAgentUtil.ets          // WantAgent创建
└──entry/src/main/resources         // 应用资源目录
```

## 参考文档

[@ohos.app.ability.wantAgent（WantAgent模块）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-app-ability-wantagent)

[@ohos.resourceschedule.backgroundTaskManager（后台任务管理）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-resourceschedule-backgroundtaskmanager)

[@ohos.notificationManager（NotificationManager模块）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-notificationmanager)

[Timer（定时器）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-timer)

[VPN应用](https://gitcode.com/openharmony/applications_app_samples/tree/master/code/DocsSample/NetWork_Kit/NetWorkKit_NetManager/VPNControl_Case)

## 代码下载

[VPN长时任务通知示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260210100051.89865944283586217284188979262184%3A50001231000000%3A2800%3A0840D214890A4BDE31B8698688C38983E358EFCC65B04BF6DD9A3C970245C24D.zip?needInitFileName=true)
