---
title: "阅读时长看板"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/time_statistics-0000002362798665
---

## 场景介绍

本示例基于[CanvasRenderingContext2D](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d)绘制阅读时长统计直方图，通过[@Watch装饰器](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-watch)刷新图表实现周、月、年等视图切换。用户可以通过阅读时长看板，查看个人阅读时长分布。

## 效果预览

![](./img/1a6c2152.png "点击放大")

## 实现思路

1. 通过按钮（周/月/年/总）切换不同时间粒度的视图，在周/月/年视图内点击左右翻页键可选择具体时间。其中BarChart为自定义组件。

   ```
   // ReadingStatsPage.ets
   Row() {
     if (this.currentView !== this.viewMap.get('all')) {
       Image($r('app.media.ic_left_one'))
         .width(10)
         .height(10)
         .onClick(() => {
           if (this.currentView === this.viewMap.get('week')) {
             // ...
           } else if (this.currentView === this.viewMap.get('month')) {
             // ...
           } else if (this.currentView === this.viewMap.get('year')) {
             // ...
           } else {
             // ...
           }
         })
     }
   }
   // ...
   // 统计卡片
   Column() {
     // 根据当前视图显示不同的图表
     Column() {
       if (this.currentView === this.viewMap.get('week')) {
         BarChart({ label: 'week', xAxisData: WEEK_AXIS, curData: this.curWeekData });
       } else if (this.currentView === this.viewMap.get('month')) {
         // ...
       } else if (this.currentView === this.viewMap.get('year')) {
         // ...
       } else if (this.currentView === this.viewMap.get('all')) {
         // ...
       }
     }
   ```
2. 点击左右翻页键后，通过[@Watch装饰器](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-watch)通知数据变更，更新阅读数据，重新生成直方图。其中ReadingDataStatic为自定义的类。

   ```
   // Chart.ets
   @Prop @Watch('onDataChange') curData: ReadingDataStatic; // 当前的阅读数据

   onDataChange() {
     this.curDataHour = this.curData.dailyMinutes.map(item => Math.floor(item / this.threshold));
     this.defOption = this.createOption()
   }
   ```
3. 通过[CanvasRenderingContext2D](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d)绘制直方图，绘制过程中根据需要计算边距、单元间隔，并按比例换算直方图的y轴数值，依次绘制分隔线、坐标轴、直方图。

   ```
   // HistogramChart.ets
   aboutToAppear() {
     this.applyOptions()
   }

   onDataChange() {
     // 使用更新后的配置绘制图形
     this.applyOptions()
     this.drawChart();
   }

   // 绘制图表
   private drawChart() {
     // ...

     // 清空画布
     CTX.clearRect(0, 0, WIDTH, HEIGHT)

     // 计算实际绘图区域（考虑所有边距）
     // 先绘制网格线，绘制完网格线后调整比例尺
     const CHART_AREA: CHART_AREA = {
       // ...
     };

     // 绘制背景（调试时可显示绘制区域）
     if (DRAWING_AREA.show) {
       // ...
     }

     // 计算最大值和比例（考虑最大高度限制）
     const MAX_DATA_VALUE = Math.max(...this.config.data, 0);
     const MAX_ALLOWED_HEIGHT = CHART_AREA.height * DRAWING_AREA.maxBarHeightRatio;
     let yScale = MAX_ALLOWED_HEIGHT / MAX_DATA_VALUE;

     // 绘制网格线
     if (this.config.gridLineStyle.show) {
       // ...
     }

     // 因为轴线标签等是在直方图外侧，最后通过padding向外进行调整，或者通过margin向内进行调整
     // 绘制Y轴
     this.drawYAxis(CTX, CHART_AREA.x, CHART_AREA.y, CHART_AREA.width, CHART_AREA.height, MAX_DATA_VALUE, yScale);

     // 绘制X轴
     this.drawXAxis(CTX, CHART_AREA.x, CHART_AREA.y, CHART_AREA.width, CHART_AREA.height);

     // 绘制柱状图
     this.drawBars(CTX, CHART_AREA.x, CHART_AREA.y, CHART_AREA.width, CHART_AREA.height, yScale);
   }
   ```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 工程目录

```
├──entry/src/main/ets             // 代码区
│  ├─component
│  │  ├─Chart.ets                 // 阅读数据、直方图绘制
│  │  └─HistogramChart.ets        // 直方图组件
│  ├──entryability
│  │  └──EntryAbility.ets         // 程序入口
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──model
│  │  ├─DailyReadingData.ets      // 阅读相关接口、类定义
│  │  ├─MockData.ets              // 阅读数据打桩
│  │  └─PersonalReadingStats.ets  // 个人阅读实例定义
│  └──pages
│     └──ReadingStatsPage.ets     // 主页面-阅读数据看板
└──entry/src/main/resources       // 应用资源目录
```

## 参考文档

[CanvasRenderingContext2D](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d)

[Drawing自绘制性能提升](https://developer.huawei.com/consumer/cn/doc/best-practices/bpta-drawing-capability-improve-performance)

[@Watch装饰器：状态变量更改通知](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-watch)

## 代码下载

[阅读时长看板示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260318091556.56623799364613267447117268623364%3A20260604135555%3A2800%3A4217548531EE881E672087DC934144A84EF82AD2B4F7D58BA403B1EEDB462F64.zip?needInitFileName=true)
