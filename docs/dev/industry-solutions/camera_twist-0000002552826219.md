---
title: "基于重力传感器实现相机图标和照片拍摄角度跟随设备旋转"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/camera_twist-0000002552826219
---

## 场景介绍

相机图标和相片拍摄角度跟随设备旋转是拍摄美化类应用的高频使用场景之一，图标跟随设备旋转始终保持在正方向可以美化拍照界面的布局设计，另外通过调整相片的拍摄角度使相片预览时始终保持正向可以更加方便用户预览相片，让用户拥有更好的拍摄体验。

由于屏幕设置了旋转锁定，本示例使用设备中的[重力传感器](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-sensor#gravity9)计算出设备当前的旋转状态，根据不同状态设置不同的旋转角度。将旋转角度分别赋予相机图标的[rotate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-transformation#rotate18)属性以及自定义相机的[PhotoCaptureSetting](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-camera-i#photocapturesetting).rotation中，来达到相机图标和照片拍摄角度跟随设备旋转的效果。

## 效果预览

![](./img/6590f05f.webp "点击放大")

## 实现思路

* **相机拍照**：参考[自定义相机拍照](https://developer.huawei.com/consumer/cn/doc/best-practices/bpta-custom-camera-photo)方法实现。
* **设备旋转状态监听**：使用重力传感器监听设备的当前旋转状态，通过检测该设备在三个坐标轴（即X轴、Y轴及Z轴）上的线性加速度，推算出设备当前的旋转状态，详情可以参考[监听屏幕旋转事件](https://developer.huawei.com/consumer/cn/doc/architecture-guides/tools-v1_2-ts_27-0000002340321377)。

  ```
  import sensor from '@ohos.sensor';
  import base from '@ohos.base';

  @Entry
  @Component
  struct OrientationPage {
    @State rotation: string = 'INVALID'

    onDegree(callback: base.Callback<string>): void {
      sensor.on(sensor.SensorId.GRAVITY, (data: sensor.GravityResponse) => {
        let degree: number = -1;
        degree = this.CalDegree(data.x, data.y, data.z)
        if (degree >= 0 && (degree <= 30 || degree >= 330)) {
          this.rotation = 'ROTATION_0';
        } else if (degree >= 60 && degree <= 120) { // Use ROTATION_90 when degree range is[60,120]
          this.rotation = 'ROTATION_90';
        } else if (degree >= 150 && degree <= 210) { // Use ROTATION_180 when degree range is[150,210]
          this.rotation = 'ROTATION_180';
        } else if (degree >= 240 && degree <= 300) { // Use ROTATION_270 when degree range is[240,300]
          this.rotation = 'ROTATION_270';
        }
        callback(this.rotation);
      });
    }

    CalDegree(x: number, y: number, z: number): number {
      let degree: number = -1;
      // 3为有效_增量_角度_阈值_系数
      if ((x * x + y * y) * 3 < z * z) {
        return degree;
      }
      degree = 90 - (Number)(Math.round(Math.atan2(y, -x) / Math.PI * 180));
      return degree >= 0 ? degree % 360 : degree % 360 + 360;
    }

    aboutToAppear() {
      let callback = async (rotation: string) => {
        console.info(`rotation is ${rotation}`)
      }
      try {
        this.onDegree(callback);
      } catch (exception) {
      }
    }
  ```

* **图标旋转**：根据设备当前的旋转状态设置图标的旋转角度。

  ```
  build() {
    Row() {
      // 相机工具界面
      Row() {
        Image($r('app.media.scope'))
          .height(20)
          .width(20)
          .rotate({ angle: this.rotation })
        Image($r('app.media.camera_filters_fill'))
          .height(20)
          .width(20)
          .rotate({ angle: this.rotation })
        Image($r('app.media.clock_close'))
          .height(20)
          .width(20)
          .rotate({ angle: this.rotation })
        Image($r('app.media.livephoto'))
          .height(20)
          .width(20)
          .rotate({ angle: this.rotation })
        Image($r('app.media.gearshape'))
          .height(20)
          .width(20)
          .rotate({ angle: this.rotation })
      }
      .backgroundColor(Color.Black)
      .width('100%')
      .height('8%')
      .justifyContent(FlexAlign.SpaceEvenly)
      .alignItems(VerticalAlign.Center)
      // 设置顶部绘制延伸到状态栏
      .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP])
    }
    .width(CameraConstants.FULL_SCREEN)
    .margin({
      top: CameraConstants.MARGIN_HEIGHT,
      bottom: CameraConstants.MARGIN_HEIGHT
    })
    .justifyContent(FlexAlign.Center);
  }
  ```
* **拍摄角度旋转**：将拍照角度写入PhotoCaptureSetting.rotation属性中，详情可以参考[适配相机旋转角度的拍照页面](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/camera-rotation-angle-adaptation#拍照)。

  ```
  let settings: camera.PhotoCaptureSetting = {
      quality: camera.QualityLevel.QUALITY_LEVEL_HIGH,
      rotation:this.rotation,
      mirror: isFront
    };
    if (this.photoOutPut) {
      this.photoOutPut.capture(settings);
    }
  ```

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 权限说明

获取使用相机权限：[ohos.permission.CAMERA](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all-user#ohospermissioncamera)。

## 工程目录

```
├──entry/src/main/ets
│  ├──component
│  │  ├──FlashBlackComponent.ets                  // 拍照闪黑组件
│  │  ├──PictureComponent.ets                     // 拍照按钮、正反拍照选择按钮、图片缩略图组合组件
│  │  ├──StackXComponent.ets                      // XComponent堆叠组件
│  │  ├──ToolsComponent.ets                       // 拍照工具组件
│  │  └──TwiceReqPermissionButtonComponent.ets    // 二次请求相机权限按钮
│  ├──constants
│  │  └──CameraConstants.ets                      // 相机相关常量
│  ├──entryability
│  │  └──EntryAbility.ets                         // 入口
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──pages
│  │  └──Index.ets                                // 相机页
│  └──utils
│     ├──CameraUtils.ets                          // 相机工具
│     └──Logger.ets                               // 日志工具
└──entry/src/main/resources                       // 应用资源目录
```

## 参考文档

[@ohos.multimedia.camera模块描述](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-camera)

[适配相机旋转角度-拍照](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/camera-rotation-angle-adaptation#拍照)

[如何监听屏幕旋转事件](https://developer.huawei.com/consumer/cn/doc/architecture-guides/tools-v1_2-ts_27-0000002340321377)

## 代码下载

[基于重力传感器实现相机图标和照片拍摄角度跟随设备旋转示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260317191936.38259814697662600130901154710624%3A20260604154822%3A2800%3AC3DD8B311AE4D9ADC870575F8073257E5B157A14DC8C00A771D25D49BE99CAF4.zip?needInitFileName=true)
