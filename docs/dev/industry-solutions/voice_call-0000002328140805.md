---
title: "语音通话页面"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/voice_call-0000002328140805
---

## 场景介绍

语音通话是社交通讯类应用的典型场景之一。

本示例基于AudioCapturer和AudioRender实现音频采集和音频渲染，通过socket完成音频数据网络传输等功能。

## 效果预览

![](./img/a3d8ac0e.png "点击放大")

## 实现思路

1. 初始化语音通话，启动定时器并注册回调，初始化音频管理器，设置扬声器和麦克风的默认状态，并播放提示音。

   ```
   init(): void {
     Logger.info(TAG, `init`);
     TimerUtil.getInstance().startTimer();
     OptionModel.registerCallback(this);
     OptionModel.subscriber();
     BufferModel.init(() => {
       // 断开流媒体服务
       this.hangUp();
     });
     AudioManagerModel.initManager();
     this.exitVoiceCall();
     // 先打开声音和mic
     this.setSpeaker(false);
     this.setMicrophoneMute(false);
     this.playSound();
     this.observer = TimerUtil.getInstance().registerObserver((count: number) => {
       this.timeoutCallback(count);
     });
   }
   ```
2. 退出语音通话时，依次执行停止音频、清除定时器、销毁模型、终止应用函数。

   ```
   exit(): void {
     Logger.info(TAG, `exit`);
     this.clearSound();
     this.exitVoiceCall(); // 停止录音和播放，切换音频场景
     TimerUtil.getInstance().stopTimer(); // 停掉计时器
     OptionModel.destroy();
     setTimeout(() => {
       BufferModel.destroy(); // 停止线程延迟200ms，给stop执行一些时间
       if (this.ctx) {
         this.ctx.terminateSelf(); // 停止应用
       }
     }, 400);
   }
   ```

![](./img/1178bafe.png)

此示例仅提供页面交互示例，通话功能需开发者自行实现。

## 约束与限制

* 本示例支持API Version 20 Release及以上版本。
* 本示例支持HarmonyOS 6.0.0 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.0 Release及以上版本进行编译运行。

## 权限说明

* 获取Internet网络权限：[ohos.permission.INTERNET](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all#ohospermissioninternet)。
* 获取数据网络信息权限：[ohos.permission.GET\_NETWORK\_INFO](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all#ohospermissionget_network_info)。
* 获取麦克风权限：[ohos.permission.MICROPHONE](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/permissions-for-all-user#ohospermissionmicrophone)。

## 工程目录

```
├──entry/src/main/ets                         // 代码区
│  ├──bean
│  │  └──Caller.ets                           // 通话人信息
│  ├──components
│  │  ├──ComponentOption.ets                  // 按钮组件
│  │  ├──ComponentPerson.ets                  // 头像和名称
│  │  ├──ComponentSingletonTimer.ets          // Timer组件
│  │  ├──ComponentVoiceBg.ets                 // 通用背景组件
│  │  ├──UIAnswer.ets                         // 被呼叫页
│  │  ├──UICall.ets                           // 呼叫页
│  │  └──UIVoice.ets                          // 音频组件
│  ├──constant
│  │  └──Constants.ets                        // 常数类
│  ├──controller
│  │  ├──FloatWindowController.ets            // 悬浮窗控制类
│  │  └──IndexController.ets                  // UI控制类
│  ├──entryability
│  │  └──EntryAbility.ets                     // 程序入口
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──model
│  │  ├──AudioManagerModel.ets                // 接口数据
│  │  ├──BufferModel.ets                      // 负责socket服务器的连接与数据传输
│  │  └──OptionModel.ets                      // 选择模块
│  ├──net
│  │  └──SocketImpl.ets                       // socket封装类
│  ├──pages
│  │  └──Index.ets                            // 首页
│  └──utils
│     ├──IpUtils.ets                          // IP类
│     ├──Logger.ets                           // 日志类
│     ├──PermissionUtils.ets                  // 授权类
│     ├──SoundUtil.ets                        // 铃声播放
│     ├──TimerUtil.ets                        // 计时器
│     └──TimeUtil.ets                         // 时间格式化类
└──entry/src/main/resources                   // 应用资源目录
```

## 参考文档

[@ohos.multimedia.audio（音频管理）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-audio)

[@ohos.bluetooth.socket（蓝牙socket模块）](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-bluetooth-socket)

## 代码下载

[语音通话页面示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260602112948.68355671223389277888513803484255%3A20260604143910%3A2800%3AAA35887EB079F64F80CCA0C563E02D0A150989CA29D52D5D9BF7EDA3EC78F81D.zip?needInitFileName=true)
