---
title: "使用Scene.load加载模型后，3D场景怎么进行手动拖拽旋转"
original_url: https://developer.huawei.com/consumer/cn/doc/architecture-guides/rotate_model-0000002556688551
---

## 场景介绍

3D模型的旋转和缩放是实用工具类应用的高频使用/典型场景之一，如查看车辆/房屋等模型时，需要切换多个视角查看模型。

本示例主要基于四元数矩阵实现模型的旋转和缩放。其中平移与旋转通过触摸事件实现，缩放通过自定义手势（捏合）事件实现。

## 效果预览

![](./img/06676b73.png "点击放大")

## 实现思路

1. 触摸事件，按压时记录x、y坐标；移动时计算x、y的变化量并更新x、y坐标的记录；手势抬起或者取消触摸事件时，清除记录。

   ```
   // 处理触摸事件
   HandleTouchEvent(event: TouchEvent): void {
     if (event.type === TouchType.Down) {
       this.OnPress(event);
     }
     if (event.type === TouchType.Up || event.type === TouchType.Cancel) {
       this.OnRelease();
     }
     if (event.type === TouchType.Move) {
       this.OnMove(event);
     }
   }

   OnPress(event: TouchEvent): void {
     if (event.touches.length === 1) {
       this.x_ = event.touches[0].x;
       this.y_ = event.touches[0].y;
       this.state = 0;
     }
   }

   OnRelease(): void {
     this.x_ = 0;
     this.y_ = 0;
     this.deltaX_ = 0;
     this.deltaY_ = 0;
     this.state = -1;
   }

   OnMove(event: TouchEvent): void {
     if (event.touches.length === 1 && this.state === 0) {
       let sensitivity: number = 0.01;
       this.deltaX_ = event.touches[0].x - this.x_;
       this.deltaY_ = event.touches[0].y - this.y_;
       this.x_ = event.touches[0].x;
       this.y_ = event.touches[0].y;
       this.UpdateCameraRotation(sensitivity * this.deltaX_, sensitivity * this.deltaY_);
     }
   }
   ```

   * UpdateCameraRotation(dx: number, dy: number)相机在水平方向上的旋转、垂直方向上的旋转，然后更新相机的四元数。

     ```
     UpdateCameraRotation(dx: number, dy: number): void {
       let rotationX: Quaternion = angleAxis(dx, { x: 0.0, y: -1.0, z: 0.0 });
       let rotationY: Quaternion = angleAxis(dy, quatMultVec3(this.cameraRotation_,
         { x: -1.0, y: 0.0, z: 0.0 }));
       this.cameraRotation_ = normalizeQuat(quatMultQuat(quatMultQuat(rotationX, rotationY), this.cameraRotation_));
     }
     ```
   * quatMultQuat四元数相乘。

     ```
     // 四元数相乘
     function quatMultQuat(q0: Quaternion, q1: Quaternion): Quaternion {
     let ret: Quaternion = {
       x: q0.w * q1.x + q0.x * q1.w + q0.y * q1.z - q0.z * q1.y,
       y: q0.w * q1.y + q0.y * q1.w + q0.z * q1.x - q0.x * q1.z,
       z: q0.w * q1.z + q0.z * q1.w + q0.x * q1.y - q0.y * q1.x,
       w: q0.w * q1.w - q0.x * q1.x - q0.y * q1.y - q0.z * q1.z
     };
     return ret;
     }
     ```
   * 归一化四元数。

     ```
     // Normalizes quaternion归一化四元数
     function normalizeQuat(q: Quaternion): Quaternion {
     let len: number = length(q);
     if (len <= 0.0) {
       return {
         x: 0.0,
         y: 0.0,
         z: 0.0,
         w: 1.0
       };
     }
     let oneOverLen: number = 1.0 / len;
     return {
       x: q.x * oneOverLen,
       y: q.y * oneOverLen,
       z: q.z * oneOverLen,
       w: q.w * oneOverLen
     };
     }
     ```
2. 处理捏合事件：通过事件的scale计算相机的距离。

   ```
   HandlePinchGestureActionUpdate(event: GestureEvent): void {
       if (!this.pinchDistance_) {
         this.pinchDistance_ = this.orbitDistance_;
       }
       let scale = event.scale;
       if (scale === 0) {
         scale = 0.01;
       }
       this.orbitDistance_ = this.pinchDistance_ * (1 / scale);
       this.state = 1;
     }
   ```

## 约束与限制

* 本示例支持API Version 22 Release及以上版本。
* 本示例支持HarmonyOS 6.0.2 Release SDK及以上版本。
* 本示例需要使用DevEco Studio 6.0.2 Release及以上版本进行编译运行。

## 工程目录

```
├──entry/src/main/ets
│  ├──entryability
│  │  └──EntryAbility.ets               // 沉浸式窗口
│  ├──entrybackupability
│  │  └──EntryBackupAbility.ets
│  ├──common
│  │  └──utils.ets                      // 工具类--包含四元数的旋转、缩放
│  └──pages
│     └──Index.ets                      // 主界面
└──entry/src/main/resources             // 应用资源目录
```

## 参考文档

[触摸事件](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-events-touch#ontouch)

[实现简单缩放](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-gestures-pinchgesture#示例1实现简单缩放)

[Node](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-inner-scene-nodes#node)

[Camera](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-inner-scene-nodes#camera)

[Quaternion](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-inner-scene-types#quaternion)

## 代码下载

[使用Scene.load加载模型后，3D场景怎么进行手动拖拽旋转示例代码](https://alliance-communityfile-drcn.dbankcdn.com/FileServer/getFile/cmtyPub/011/111/111/0000000000011111111.20260312185345.01125220943301225612990843062469%3A20260604150238%3A2800%3AC03B18444B91C9EE3DC9108803E548F3F703138285739B03BF8E6C7F2F920796.zip?needInitFileName=true)
